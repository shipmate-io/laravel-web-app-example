service:
  name: backend
  type: 'laravel:1.3'
  variables:
    path_to_source_code: /backend
    php_version: '8.1'
    deploy_script: |-
      php artisan config:cache
      php artisan route:cache
      php artisan view:cache
  environment:
    - APP_KEY